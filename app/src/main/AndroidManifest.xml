<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.practicaltest">

    <application
        android:name=".core.util.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PracticalTest">
        <activity
            android:name=".app.ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.PracticalTest.NoActionBar"/>
        <activity
            android:name=".core.ui.CoreActivity"
            android:exported="true"
            android:theme="@style/Theme.PracticalTest.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.ui.MainActivity"
            android:exported="true" />
    </application>

</manifest>